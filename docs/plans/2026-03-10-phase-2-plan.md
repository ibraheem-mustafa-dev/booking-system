════════════════════════════════════════════════
📋 PLAN: Booking System — Phase 2
Created: 2026-03-10
Deadline: Soft — ASAP, quality-first
Scheduled: Continuous — Blub runs agents, Bean reviews sprints
Estimated total time: 7 sprints across ~2 weeks
════════════════════════════════════════════════

🎯 GOAL
Make the booking system publicly usable for SGS — clients discover
it on LinkedIn or the SGS website, book a slot, pay via Stripe,
and self-manage (reschedule/cancel) with zero manual input from Bean.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONFIRMED DECISIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Public domain: book.smallgiantsstudio.co.uk (not .cloud)
- Single-host: SGS only, no multi-tenant for now
- Calendar: Google Calendar API (Work calendar) — not Radicale
- URL structure: /[typeSlug] (drop /book/[orgSlug]/ layer)
- Embed: iframe widget for WordPress SGS site on Hostinger
- Shortlinks: book.smallgiantsstudio.co.uk/[typeSlug] (LinkedIn-ready)
- Implementation: Blub runs coding agents. Bean reviews each sprint.

⚠ RISKS — DEAL WITH THESE FIRST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔴 STRIPE_WEBHOOK_SECRET not yet set on VPS — Stripe payments
   won't work until this is added to .env.production after
   registering the webhook endpoint in Stripe dashboard.
   Action: After Sprint 6, Bean logs into Stripe dashboard,
   creates webhook for book.smallgiantsstudio.co.uk/api/webhooks/stripe,
   copies secret into .env.production on VPS.

🔴 Google Calendar OAuth tokens — gog tokens for
   ibraheemmustafa16@gmail.com were lost from Windows keyring
   (confirmed 2026-03-10). Calendar integration will fail silently
   until Bean re-runs `gog auth add`.
   Action: Bean re-authenticates before Sprint 5.

🟠 WordPress embed CORS — the SGS site is on Hostinger (different
   domain to book.smallgiantsstudio.co.uk). CORS headers must be
   correctly set or the iframe won't load. Blub handles this in
   Sprint 2 but needs to verify from external URL after deploy.

🟡 Supabase Auth redirect URLs — must be updated to include
   https://book.smallgiantsstudio.co.uk before Sprint 1 is
   considered done. Otherwise login/magic links will break.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 SPRINT 1 — Domain & Infrastructure
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Goal: book.smallgiantsstudio.co.uk is live with HTTPS.
Blub runs this. Bean verifies the URL loads after.

[ ] 1a — Add DNS A record in Cloudflare
    book.smallgiantsstudio.co.uk → 72.62.212.169
    ⏱ 5 min | Done when: DNS resolves (allow 5 min TTL propagation)

[ ] 1b — SSH to VPS: run Certbot for new domain
    certbot --nginx -d book.smallgiantsstudio.co.uk
    ⏱ 10 min | Done when: SSL cert issued, no errors

[ ] 1c — Update nginx: add server block for new domain
    ⏱ 10 min | Done when: nginx -t passes, service reloaded

[ ] 1d — Update .env.production on VPS
    NEXT_PUBLIC_APP_URL=https://book.smallgiantsstudio.co.uk
    ⏱ 5 min | Done when: variable updated, containers restarted

[ ] 1e — Update Supabase Auth redirect URLs
    Add https://book.smallgiantsstudio.co.uk/** to allowed URLs
    in Supabase dashboard (Authentication > URL Configuration)
    ⏱ 5 min | Done when: saved in Supabase dashboard

[ ] 1f — Smoke test
    curl -I https://book.smallgiantsstudio.co.uk → 200 OK
    Magic link login works end-to-end
    ⏱ 10 min | Done when: login confirmed working on new domain

🛑 CHECKPOINT — Bean opens book.smallgiantsstudio.co.uk in browser.
Logs in. Confirms it works. Green light → Sprint 2.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 SPRINT 2 — URL Simplification + Embed Widget
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Goal: Clean shortlinks + iframe embed ready to paste into WordPress.
⚠ Depends on: Sprint 1 complete

[ ] 2a — Simplify public booking URL structure
    Add redirects or restructure routes so that:
    /[typeSlug] → loads the booking page for that type
    /book/[orgSlug]/[typeSlug] → 301 redirect to /[typeSlug]
    (backwards-compatible — old links still work)
    ⏱ 45–90 min | Done when: book.smallgiantsstudio.co.uk/strategy works

[ ] 2b — Add CORS headers for embed
    In next.config.ts, add headers() to allow embedding from
    smallgiantsstudio.co.uk. Set X-Frame-Options: ALLOWALL or
    Content-Security-Policy: frame-ancestors 'self' *.smallgiantsstudio.co.uk
    ⏱ 20 min | Done when: iframe loads from external domain

[ ] 2c — Create embed snippet documentation
    Generate a standard iframe snippet Bean can paste:
    <iframe src="https://book.smallgiantsstudio.co.uk/[typeSlug]"
      width="100%" height="700" frameborder="0"></iframe>
    And a WordPress shortcode if SGS theme supports it.
    ⏱ 20 min | Done when: snippet documented in README or embed guide

[ ] 2d — Deploy + verify embed
    Test iframe loads correctly on an external page.
    ⏱ 15 min | Done when: visual confirm iframe renders booking form

🛑 CHECKPOINT — Bean can copy-paste a shortlink into LinkedIn bio.
Iframe snippet ready to add to SGS site. Green light → Sprint 3.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 SPRINT 3 — Client Self-Service (Cancel + Reschedule)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Goal: Clients can cancel or reschedule from their confirmation email.
No login required. PRD: prd-client-self-service.md

[ ] 3a — Token validation helper
    src/lib/booking-tokens.ts
    getBookingByCancellationToken(token)
    getBookingByRescheduleToken(token)
    ⏱ 20 min

[ ] 3b — Cancel API route
    POST /api/v1/booking/cancel { token }
    Validates token → updates status to 'cancelled' → sends
    host notification email → removes BullMQ reminder jobs
    ⏱ 45–60 min

[ ] 3c — Cancel page
    /book/cancel?token=... 
    Shows booking summary + confirm button → success state
    ⏱ 45 min

[ ] 3d — Reschedule API route
    POST /api/v1/booking/reschedule { token, startAt, endAt }
    Validates token → checks availability → updates booking →
    sends updated confirmation email to client + notification to host
    ⏱ 60–90 min

[ ] 3e — Reschedule page
    /book/reschedule?token=...
    Shows existing booking at top → date/time picker below
    (reuse existing booking form components)
    ⏱ 60–90 min

[ ] 3f — Wire tokens into confirmation email
    Verify cancellationToken and rescheduleToken are included in
    the existing confirmation email template as clickable links
    ⏱ 20 min

🛑 CHECKPOINT — Bean books a test appointment, receives email,
clicks Reschedule, picks a new time. Clicks Cancel on another.
Both flows work end-to-end. Green light → Sprint 4.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 SPRINT 4 — Dashboard Overview + Booking Type Management
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Goal: Dashboard is useful on first open. Booking types are editable.
PRDs: prd-dashboard-overview.md, prd-booking-type-management.md

[ ] 4a — Dashboard stats tRPC endpoint
    getOverview: upcoming 7 days, last 5 bookings, this month
    count + revenue, outstanding invoices count + total
    ⏱ 45–60 min

[ ] 4b — Dashboard overview page
    Replace empty home page. 4 stat cards (bookings, revenue,
    outstanding invoices, recordings). Upcoming this week table.
    Recent activity feed. Quick action buttons.
    ⏱ 90–120 min

[ ] 4c — Booking type edit page
    /dashboard/booking-types/[id]/edit
    Full form: name, description, duration, price, colour, active toggle
    ⏱ 60–90 min

[ ] 4d — Delete booking type
    With guard: check for future bookings before allowing delete
    ⏱ 30–45 min

[ ] 4e — Duplicate booking type
    One-click duplicate with "Copy of [name]"
    ⏱ 20–30 min

🛑 CHECKPOINT — Bean opens dashboard. Sees real stats.
Edits a booking type, duplicates it, deletes a test one.
Green light → Sprint 5.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 SPRINT 5 — Google Calendar + Email Settings UI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Goal: Bookings appear in Google Calendar. Email follow-ups configurable.
⚠ Depends on: Bean re-authenticates gog for ibraheemmustafa16@gmail.com
PRD: prd-email-settings-ui.md

[ ] 5a — Google Calendar integration (server-side)
    On booking confirmed: create a Google Calendar event in
    Work calendar (d6d3603e...) using googleapis SDK.
    Event includes: client name, booking type, start/end time,
    location/video link (if set on booking type).
    On booking cancelled: delete the event.
    On booking rescheduled: update the event time.
    ⏱ 90–120 min

[ ] 5b — Google Calendar credentials on VPS
    Add GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET + refresh token
    for ibraheemmustafa16@gmail.com to .env.production on VPS
    ⏱ 20 min (after Bean re-auths gog)

[ ] 5c — Email Settings UI
    /dashboard/booking-types/[id]/edit → add "Email Notifications" section
    Two collapsible panels: Review Request + Follow-up Reminder
    Enable/disable toggle, delay picker, subject + body fields
    with placeholder variable hints ({{clientName}}, etc.)
    ⏱ 90–120 min

[ ] 5d — Wire email settings into worker
    Verify BullMQ worker actually reads emailSettings JSONB
    and uses custom subject/body when sending follow-ups
    ⏱ 30–45 min

🛑 CHECKPOINT — Bean confirms a test booking shows in his
Google Calendar within 30 seconds of booking. Email settings
UI saves and custom templates send correctly. Green light → Sprint 6.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 SPRINT 6 — Stripe Invoice Payments
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Goal: Clients can pay invoices online. Status auto-updates on payment.
PRD: prd-stripe-payments.md
⚠ Human step required: Bean registers webhook in Stripe dashboard
  after deploy, copies STRIPE_WEBHOOK_SECRET to .env.production on VPS.

[ ] 6a — Stripe client singleton
    src/lib/stripe.ts — lazy-init singleton
    ⏱ 10 min

[ ] 6b — Schema additions
    invoices table: stripePaymentIntentId, stripeCheckoutSessionId, paidAt
    Run drizzle-kit generate + migrate on VPS
    ⏱ 20–30 min

[ ] 6c — Create Checkout Session API
    POST /api/invoices/[id]/checkout
    Creates Stripe Checkout Session, stores session ID, returns URL
    success_url: /invoice/[id]/paid | cancel_url: /invoice/[id]
    ⏱ 45–60 min

[ ] 6d — Invoice page "Pay Now" button
    On client-facing invoice view, add "Pay Now" → hits checkout API → redirect
    ⏱ 30 min

[ ] 6e — Stripe webhook handler
    POST /api/webhooks/stripe
    Verifies signature → handles checkout.session.completed →
    updates invoice status to 'paid', sets paidAt
    ⏱ 45–60 min

[ ] 6f — Manual "mark as paid" update
    Confirm existing dashboard manual-paid flow still works
    alongside Stripe auto-update
    ⏱ 15 min

[ ] 6g — Deploy + register webhook
    Push to VPS. Bean logs into Stripe dashboard, registers:
    https://book.smallgiantsstudio.co.uk/api/webhooks/stripe
    Copies STRIPE_WEBHOOK_SECRET → .env.production → restart containers.
    ⏱ 15 min (Bean action required)

🛑 CHECKPOINT — Bean sends a test invoice, clicks Pay Now,
completes Stripe test checkout. Invoice status updates to paid
automatically. Green light → Sprint 7.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 SPRINT 7 — QA, Polish + Summary Sharing
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Goal: Production-ready. Bean can use this with real clients.

[ ] 7a — Mobile-first review
    Test all flows on mobile viewport. Fix any layout issues.
    ⏱ 60–90 min

[ ] 7b — Email template polish
    Review all outgoing email templates. Ensure brand colours,
    correct links (using new domain), mobile-readable layout.
    ⏱ 45–60 min

[ ] 7c — Recording Summary Share (PRD: prd-summary-sharing.md)
    Public /share/recording/[token] page. Client views AI summary.
    Host sees "viewed" status on recording detail page.
    ⏱ 90–120 min

[ ] 7d — End-to-end walkthrough
    Full flow: public books via shortlink → confirmation email →
    reschedule → invoice sent → client pays → cancelled booking.
    All green.
    ⏱ 30 min

[ ] 7e — Deploy final build to VPS
    Run migrations, restart containers, smoke test on live domain.
    ⏱ 20 min

🛑 FINAL CHECKPOINT — Bean puts shortlink in LinkedIn bio.
Adds iframe to SGS website. Phase 2 is live.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏁 DONE STATE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Phase 2 is complete when:
  ✓ book.smallgiantsstudio.co.uk is live with HTTPS
  ✓ /[typeSlug] shortlinks work and are LinkedIn-ready
  ✓ iframe embed working on SGS WordPress site
  ✓ Clients can cancel and reschedule via email token links
  ✓ Confirmed bookings push to Bean's Google Work Calendar
  ✓ Dashboard shows real stats on first open
  ✓ Booking types are editable/deletable/duplicatable
  ✓ Invoices have a working "Pay Now" Stripe button
  ✓ Stripe webhook auto-marks invoices as paid

════════════════════════════════════════════════
⏱ TIME REALITY CHECK
Raw estimate: ~30 hrs of agent work
ADHD tax: N/A (Blub runs agents, no ADHD tax)
Constraint: Agent rate limits + Bean review turnaround
Realistic timeline: 1–2 weeks at current pace
Sprint order is deliberate — each sprint ships visible value
even if later sprints are delayed.
════════════════════════════════════════════════

BEAN ACTION ITEMS (human-only):
  After Sprint 1: Verify new domain loads in browser
  After Sprint 3: Test cancel + reschedule flows manually
  Before Sprint 5: Re-run `gog auth add` for ibraheemmustafa16@gmail.com
  During Sprint 6: Register Stripe webhook in dashboard, add secret to VPS
  After Sprint 7: Add shortlink to LinkedIn + iframe to SGS site
